# Working agreement
- Ethan implements the agent loop, sandbox runner, eval runner, monitor, and attack policy.
- Tests are the spec. Do not replace TODOs with copied ControlArena code.
- Commit configs before runs. Record seed, model, prompt, dependency lock, and commit with results.
- Use toy side tasks only; never test sabotage against real systems or credentials.
